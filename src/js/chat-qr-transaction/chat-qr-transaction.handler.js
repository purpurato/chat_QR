'use strict'

const fs = require('fs');
const path = require('path');
const os = require('os');
const qs = require('querystring');

module.exports = function(server, conf){
	class ChatQrTransactionHandler {
		constructor(){
		}

		
	}
	return new ChatQrTransactionHandler();
}